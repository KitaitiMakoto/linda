{
  "dependencies": {
    "bower": "*",
    "vulcanize": "*"
  },
  "scripts": {
    "postinstall": "PATH=./node_modules/.bin:$PATH bower install && bower prune && vulcanize -o ./public/app.html ./public/app.src.html"
  }
}
