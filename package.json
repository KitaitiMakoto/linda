{
  "dependencies": {
    "bower": "*",
    "vulcanize": "*"
  },
  "scripts": {
    "postinstall": "PATH=./node_modules/.bin:$PATH bower install && bower prune && vulcanize -o ./public/index.html ./public/index.src.html"
  }
}
